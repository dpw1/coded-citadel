export default function BlogCoverFallback({ title }) {
  return (
    <div
      className="CC__blog-cover-fallback"
      role="img"
      aria-label={`Coded Citadel placeholder cover for ${title}`}
    />
  )
}
